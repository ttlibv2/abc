import { OutputSetting } from '../parse/Setting';
import { StringBuilder } from './StringBuilder';

export class Entities {
	static unescape(encodedText: string): string {
		throw new Error('Method not implemented.');
	}
	static escape(accum: StringBuilder, coreVal: string, setting: OutputSetting, arg3: boolean, normaliseWhite: boolean, stripWhite: boolean) {
		throw new Error('Method not implemented.');
	}
}
